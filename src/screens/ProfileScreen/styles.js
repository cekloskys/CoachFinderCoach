import { StyleSheet, Dimensions } from 'react-native';

const styles = StyleSheet.create({
    page: {
      padding: 10,
    },
    header: {
      margin: 10,
    },
    title: {
      fontSize: 45,
      fontWeight: 'bold',
      position: 'absolute',
      top: 0,
      color: 'black',
      width: '100%',
      textAlign: 'center',
    },
  });

  export default styles; 